/**
 * Legacy entry point — kept for compatibility only.
 *
 * The server has moved to src/server/server.ts.
 * Run it with:
 *   npm run server          (dev — tsx watch)
 *   npm run server:prod     (production — compiled JS)
 */
console.warn('[eigenself] server.js is deprecated. Use `npm run server` instead.');

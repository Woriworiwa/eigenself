export const environment = {
  production: true,
  // API + Socket.IO server on EC2, proxied through nginx with SSL.
  // nip.io gives a free hostname for any IP — no domain purchase needed.
  apiUrl: 'https://13.223.248.251.nip.io',
};
